# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
env/

# uv
.uv/

# pytest / coverage (generated reports — regenerated in CI for Sonar)
.pytest_cache/
.coverage
htmlcov/
coverage.xml
web/coverage/

# Downloads (runtime data — never commit downloaded files)
downloads/*
!downloads/.gitkeep

# VPN configs (contain WireGuard private keys — NEVER commit)
vpn_configs/*
!vpn_configs/.gitkeep

# Docker
.docker/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Env files
.env
.env.*
# ...but the documented template is checked in.
!.env.example

# Logs
logs/

# SQLite database (runtime)
smuggler.db
smuggler.db-wal
smuggler.db-shm

# Node / Web
web/node_modules/
web/dist/

# Temp VPN config uploads (private keys)
/tmp/smuggler_*

# Runtime data directory — SQLite DB + temp VPN configs with private keys.
# NEVER commit: data/tmp/ holds uploaded WireGuard/OpenVPN configs in plaintext.
data/

# Tools / AI
.claude/
.codegraph/

# Desktop (Gradle)
desktop/build/
desktop/.gradle/
desktop/gradle/wrapper/gradle-wrapper.jar.lock

coverage.json
